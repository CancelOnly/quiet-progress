# /etc/cloudflared/config.yml
# Troque <TUNNEL_UUID> e o hostname.

tunnel: <TUNNEL_UUID>
credentials-file: /etc/cloudflared/<TUNNEL_UUID>.json

ingress:
  - hostname: quiet.seu-dominio.com
    service: http://127.0.0.1:3000
  - service: http_status:404
